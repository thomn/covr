{
  "name": "backend",
  "scripts": {
    "coverage": "nyc npm test",
    "test": "mocha -r tsconfig-paths/register -r ts-node/register/transpile-only --full-trace 'tests/**/*.ts'",
    "test:watch": "mocha -w"
  },
  "dependencies": {
    "conr": "^0.1.6",
    "fdex": "^0.1.2",
    "fsbr": "^1.0.11",
    "mongoose": "^6.7.2",
    "sevl": "^0.1.7",
    "teda": "^0.1.6",
    "@sentry/node": "^7.19.0"
  }
}
